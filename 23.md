# 23. Merge k Sorted Lists

## 📝 Problem Statement

You are given an array of `k` linked lists, where each linked list is sorted in ascending order.

Merge all the linked lists into one sorted linked list and return it.

**LeetCode Link:** https://leetcode.com/problems/merge-k-sorted-lists/

---

## 💡 Approach: Min Heap (Priority Queue)

Since every linked list is already sorted, we can efficiently merge them using a **Min Heap (Priority Queue)**.

### Algorithm

1. Create a min heap to store the current smallest node from each linked list.
2. Insert the head node of every non-empty linked list into the heap.
3. Create a dummy node for the result list.
4. While the heap is not empty:
   - Remove the smallest node.
   - Append it to the merged list.
   - If the removed node has a next node, push it into the heap.
5. Return `dummy->next`.

---

## ✅ C++ Solution

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */

class Solution {
public:
    struct Compare {
        bool operator()(ListNode* a, ListNode* b) {
            return a->val > b->val;
        }
    };

    ListNode* mergeKLists(vector<ListNode*>& lists) {
        priority_queue<ListNode*, vector<ListNode*>, Compare> pq;

        for (auto node : lists) {
            if (node)
                pq.push(node);
        }

        ListNode dummy(0);
        ListNode* tail = &dummy;

        while (!pq.empty()) {
            ListNode* node = pq.top();
            pq.pop();

            tail->next = node;
            tail = tail->next;

            if (node->next)
                pq.push(node->next);
        }

        return dummy.next;
    }
};
```

---

## 🔍 Dry Run

### Input

```text
lists = [[1,4,5],[1,3,4],[2,6]]
```

### Heap Operations

| Heap Top | Result List |
|----------|-------------|
| 1 | 1 |
| 1 | 1 → 1 |
| 2 | 1 → 1 → 2 |
| 3 | 1 → 1 → 2 → 3 |
| 4 | 1 → 1 → 2 → 3 → 4 |
| 4 | 1 → 1 → 2 → 3 → 4 → 4 |
| 5 | 1 → 1 → 2 → 3 → 4 → 4 → 5 |
| 6 | 1 → 1 → 2 → 3 → 4 → 4 → 5 → 6 |

### Output

```text
[1,1,2,3,4,4,5,6]
```

---

## 💡 Why Use a Min Heap?

At any moment, the smallest element among the heads of all linked lists should be chosen next.

A min heap allows us to:

- Retrieve the smallest node in `O(log k)` time.
- Insert the next node in `O(log k)` time.
- Process all `N` nodes efficiently.

---

## ⏱️ Complexity Analysis

Let:

- `N` = Total number of nodes across all linked lists
- `k` = Number of linked lists

### Time Complexity

- Building heap: `O(k)`
- For each node:
  - Pop: `O(log k)`
  - Push: `O(log k)`

Overall:

```text
O(N log k)
```

### Space Complexity

```text
O(k)
```

for the priority queue.

---

## 🔄 Alternative Approach

### Divide and Conquer

Merge the linked lists in pairs, similar to Merge Sort.

- Merge pairs of lists repeatedly.
- Reduce the number of lists by half each iteration.

**Time Complexity:** `O(N log k)`

**Space Complexity:** `O(1)` (excluding recursion stack)

---

## 🏷️ Tags

- Linked List
- Heap (Priority Queue)
- Divide and Conquer
- Merge Sort

---

⭐ **If you found this solution helpful, don't forget to star the repository!**
